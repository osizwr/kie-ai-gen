{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Generate images",
      "type": "debugpy",
      "request": "launch",
      "program": "${workspaceFolder}/generate_images.py",
      "cwd": "${workspaceFolder}",
      "console": "integratedTerminal",
      "justMyCode": true
    }
  ]
}
